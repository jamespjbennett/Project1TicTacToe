<h1>YOU LOSE!!</h1>
<h3>You now have <%=current_user.wins %> wins, <%=current_user.draws%> draws and <%=current_user.losses%> losses. </h3>
<%= link_to('New Game', new_ttt_path) %>
<%= link_to('User Leaderboard', users_path) %>